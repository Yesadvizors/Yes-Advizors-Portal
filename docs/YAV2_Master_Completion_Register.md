# YAV2 Portal V2 — Master Completion Register

Rev 1.3 · Created 2026-07-18 (P0 discovery); updated same day with P0 review rulings + housekeeping; updated 2026-07-21 21:53 IST (P5 UI live runtime verification Steps 1–14 PASS); **finalised 2026-07-21 22:44 IST — P5 and Module 1 CLOSED PASS; Independent ChatGPT review PASS; PJ final approval granted 2026-07-21 IST** · Owner: PJ · Executor: Claude Code · Reviewer: ChatGPT

Governed repository: `D:\Claude\Claude Code\Yes-Advizors-Portal` (GitHub `Yesadvizors/Yes-Advizors-Portal`)
Working branch (historical P5/Module-1 closure anchor): `ui/redesign-v1` @ `3a5f439c15cafa493cd2d2320d7733f286441b6b` (governing HEAD for P5 runtime closure; earlier revisions of this register cited `2841664…`) — retained only as the historical governing anchor for the P5/Module-1 closure.
Current integration continuation branch for post-Module-1 work: `sync/integration` @ `b1f2b60d52a0d09a9561e06182b5ced397ff8439`.
Authorized Supabase target: **V2 / yav2-dev `ogjrwemjefvccpyjwxuo` only**. V1/Production `zcszesuvjrryxtigjglt` prohibited.

Status vocabulary: NOT STARTED · DISCOVERY · AUTHORED · REVIEW HOLD · APPROVED FOR EXECUTION · EXECUTED · VERIFIED · CLOSED / PASS · BLOCKED

---

## 0. Closed baseline (immutable)

| Item | Status |
|---|---|
| Portal V2 repo, Preview deployment, PJ admin login | CLOSED / PASS |
| R4-DB migration 0014 (FY 2020-21…2030-31, current FY 2026-27, coverage repair) | CLOSED / PASS — **never modify** |
| Calendar concurrency-safe upsert in Preview | CLOSED / PASS |
| M1-A migration 0015 (9 Client Master tables, clients.id UUID key, RLS forced, 12 audit events) | CLOSED / PASS |
| M1-B D1 discovery | CLOSED / PASS |
| M1-B D2a migration 0016 (audit_write_event, 6 family events, lineage + remediation columns/indexes) | CLOSED / PASS — **never rerun/modify** |
| D2a live verification (owner postgres, SECURITY DEFINER, pinned search_path, no direct EXECUTE, protected counts unchanged) | CLOSED / PASS |

Protected counts (guard values for every future verification): clients 13 · accounting_tracker 312 · financials_tracker 120 · income_tax_tracker 26 · compliance_calendar 0 · client_persons 0 · client_remediation_flags 0.

---

## P0 — Repository and live-state reconciliation

- **Workstream:** Governance / discovery
- **Objective:** Reconcile repo, Preview and live state; produce this register and the P0 package.
- **Scope:** Read-only/local only. No SQL, no deploy, no P1 authoring.
- **Dependencies:** none.
- **Database objects:** none touched. Zero database writes; zero database reads (see blocker B-1).
- **Frontend files:** none changed.
- **Execution status:** EXECUTED (this session, 2026-07-18).
- **Review status:** **CLOSED / PASS WITH CONDITIONS** (ChatGPT independent review, 2026-07-18). Rulings recorded in §P0 Rulings below. Post-review housekeeping EXECUTED same day — see §P0 Housekeeping record.
- **Commit:** `c67d65093426d0d21ae42fd08218e0a552f95811` — `chore(db): record verified M1-B D2a audit foundation` (the four closed D2a artifacts only; local, not pushed).
- **Preview URL:** `yes-advizors-portal-v2-preview-3gqbxbkmy-yes-advizors-projects.vercel.app` (latest READY deployment).
- **Rollback position:** n/a (no state changed).
- **% complete:** 100 % of what is locally reconcilable; live-DB reconciliation 0 % (BLOCKED, B-1).
- **Blockers:** B-1, B-2, B-3 (see §Risk register).
- **Next action:** independent review; PJ decisions on B-1/B-2/B-3.

### P0 Rulings (independent review, 2026-07-18)

- **B-1 — ruled EXECUTION BLOCKER ONLY.** The MCP connector exposes prohibited V1/Production `zcszesuvjrryxtigjglt`; it must never be queried. Local authoring continues. Live V2 verification and approved execution use **PJ's manual V2 SQL Editor** unless a safe V2-only connector becomes available.
- **B-2 — provenance CONFIRMED.** The 0017 file was authored in the earlier D2b session. Classification: **D2b Rev 1 — HOLD / REVISION REQUIRED.** Retain; do not delete, execute, or treat as unknown.
- **B-3 — APPROVED (scoped).** Remove only the `supabase/` line from `.git/info/exclude`; then inspect and classify the full tree before staging. No blind commit of `migrations.zip` or duplicates. Historical migration corpus NOT committed yet.
- **R-6 — RULED.** Portal V2 must not capture, accept, backfill, or newly store Aadhaar digits including last-four. No destructive deletion of legacy source fields during P1/P3; legacy cleanup separately governed.

### P0 Housekeeping record (executed 2026-07-18, post-review)

1. **D2a integrity check: PASS.** All four staged D2a artifacts byte-identical between index and working tree. SHA-256:
   - `docs/M1B_D2a_Implementation_Report.md` — `91fb54c67b500f336853517f116fc3f45a27d1218fd9331b8340841523f54d14`
   - `supabase/migrations/0016_m1b_d2a_audit_write_and_lineage.sql` — `a5044b4e2672ebc4d4947a142c82fb5edc546160d806f68b8213c4fbf76f8c40`
   - `supabase/migrations/0016_m1b_d2a_audit_write_and_lineage_rollback.sql` — `83c722704689c889668fcd851578eab3b2c51a9c83794a850fa272d74172a90a`
   - `supabase/verification/M1B_D2a_post_execution_verification_readonly.sql` — `aa4965d2d21df6d82e281b1985111ccd2652bc7c83d6a4bd1848040c89d08842`
2. **D2a commit:** `c67d65093426d0d21ae42fd08218e0a552f95811` (4 files, 1,220 insertions; pathspec-limited commit; local only, not pushed).
3. **`.git/info/exclude`:** `supabase/` line removed (backup kept at `.git/info/exclude.bak-p0`). No other line touched.
4. **DEVIATION D-P0-1 (recorded, no action taken):** between the P0 snapshot and housekeeping, the **concurrent D2b session** continued working in this tree (07:03–07:08 IST): it modified `0017` again (61,942 → 61,873 bytes), authored `0017_…_rollback.sql` (07:04), `M1B_D2b_post_execution_verification_readonly.sql` (07:06), `docs/M1B_D2b_Implementation_Report.md` (07:07), and **staged all four D2b files** (index 07:08). This executor did not stage, modify, or unstage any D2b file; the D2a commit was pathspec-limited so D2b's staged state is exactly as that session left it. D2b Rev 1 current hashes (read-only record):
   - `0017_m1b_d2b_client_master_crud_rpcs.sql` — `b72c4a63b88067e35fda01124354f2a400207c625ffdd3e45852a3ff0ffb1db4`
   - `0017_m1b_d2b_client_master_crud_rpcs_rollback.sql` — `d3848d3d9b055c26692fe7310117318e34ccae9502c6044abf57b29898e26180`
   - `supabase/verification/M1B_D2b_post_execution_verification_readonly.sql` — `052e77345f920aa93de0edfc32453b9fd61bcffc3b18b7192eda7b75cd040bd2`
   - `docs/M1B_D2b_Implementation_Report.md` — `4092740ef9ba70e5e56c245b5f9ce9df6dc3a5073e85ffd51706d3b8c24ed386`

   **Whether these four staged D2b files remain staged is a review decision; recommendation: unstage (index-only, content untouched) to honour the HOLD / REVISION REQUIRED status.**

### Classified Supabase inventory (newly visible after exclude removal)

| Class | Files | Disposition |
|---|---|---|
| Authoritative historical migrations (V1-parity corpus, G-2b Rev1.2, applied to V2 via G-3) | 0001–0011 (12 files incl. 0011 DEFER) | Untracked. Commit later as a reviewed hygiene commit (ruling 5: not yet). |
| Authoritative executed gate migrations | 0014 + 0014_rollback (untracked); 0015 + rollback (committed `2841664`); 0016 + rollback (committed `c67d650`) | 0014 pair to join the hygiene commit. |
| D2a artifacts | 0016 pair + verification + report | **Committed `c67d650`.** |
| D2b draft (Rev 1 — HOLD / REVISION REQUIRED) | 0017 + 0017_rollback + D2b verification + D2b report | Retained, frozen; currently staged by the D2b session (D-P0-1). Not committed. |
| Verification files (closed gates) | M1A (committed), M1B_D2a (committed) | Done. |
| Archives / duplicates / temporary | `supabase/migrations.zip` (65,297 B, 2026-07-16 snapshot of 0001–0015 — pure duplicate) | Exclude from any commit; recommend delete or move outside repo (needs approval). `.git/info/exclude.bak-p0` is temporary — delete after review confirms. |

SHA-256 of the newly visible corpus: 0001 `9f76338f…`, 0002 `ccf200f8…`, 0003 `27dc65b0…`, 0004 `f38a1eaa…`, 0005 `2e3693f4…`, 0006 `705bd59a…`, 0007 `2506ccaf…`, 0008 `d5797786…`, 0009 `ba4bf408…`, 0010 `5d20953b…`, 0011 `ef947d42…`, 0014 `c32c8a0c…`, 0014_rollback `d2120091…`, migrations.zip `7558f2c2…` (full 64-hex values in the housekeeping session transcript).

### Proposed repository-hygiene commit plan (for approval — NOT executed)

1. `chore(db): record V1-parity migration corpus 0001-0011 (G-2b Rev1.2, applied via G-3)` — the 12 historical files, byte-exact.
2. `chore(db): record executed R4-DB migration 0014 and rollback` — the 0014 pair.
3. `docs: add Master Completion Register` — this register.
4. Delete `supabase/migrations.zip` (duplicate) — or relocate outside the repo.
5. (After D2b review completes its own gate) D2b files enter git under the P1 gate, never under housekeeping.

### P0 findings — git state

- Branch `ui/redesign-v1`, HEAD `2841664` ("M1-A Rev1.1: finalize client-master migration, rollback, verification kit"), in sync with `origin/ui/redesign-v1` (0 ahead / 0 behind). No stash.
- **Staged, uncommitted (4 files — D2a evidence):** `docs/M1B_D2a_Implementation_Report.md`, `supabase/migrations/0016_….sql`, `0016_…_rollback.sql`, `supabase/verification/M1B_D2a_post_execution_verification_readonly.sql`.
- No unstaged modifications.
- **`.git/info/exclude` contains `supabase/`** (local, non-versioned). This hides the entire `supabase/` tree from `git status`. Consequence: migrations 0001–0011 and the 0014 pair exist **on disk only** — they are in no commit on any branch. Only 0015 (committed) and 0016 (staged) entered git, evidently via forced adds. `supabase/migrations.zip` also on disk only.
- **Out-of-band artifact:** `supabase/migrations/0017_m1b_d2b_client_master_crud_rpcs.sql` (61,942 bytes) appeared on disk at **2026-07-18 07:03:38 IST — during this P0 session, seconds after the first directory listing**. It was NOT authored by this session. Header claims a complete D2b package (19 RPCs + bypass closure) targeting 0017. Provenance unknown to the executor. Untracked, hidden from status by the `supabase/` exclude. **Quarantine pending review (B-2).**
- Local branches: `main`, `ui/redesign-v1`, `fix/frontend-safety-v1`, `g2b/v2-migrations` (at ae6bf1e0, behind working branch). Numerous historical `feat/*`, `claude/*`, `tmp/*` branches on origin (phase-4 audit series, dkyc series) — superseded history, no active work found there.

### P0 findings — Preview deployment (Vercel, read-only)

- Project `yes-advizors-portal-v2-preview` (`prj_PFPT5rOJ4hpjyfqDHvppBlxTVeZv`, team `team_k1XHDoYfk0zOBVvGC4pVPFOc`).
- Latest deployment `dpl_5NhXDhficfZ1t2DY28NrZfYtUMGQ`: state READY, created ≈ 2026-07-11, built from **`g2b/v2-migrations` @ `ae6bf1e0`** (PR #12 baseline), flag `gitDirty: 1`.
- **Preview is 13 commits behind `ui/redesign-v1` HEAD** — none of the frontend-hardening, R4 FY, onboarding-draft, Aadhaar-guard or M1-A-era frontend commits (`bda50f6`…`2841664`) are deployed.

### P0 findings — live database state

- **BLOCKED (B-1):** the connected Supabase MCP server exposes **only** V1/Production `zcszesuvjrryxtigjglt`. Per Global Rule 2 the executor stopped immediately; **zero queries were run against any project** (only the tool's project list was read). Live-state description below therefore rests on the CLOSED D2a live-verification evidence (2026-07-17), which remains the authoritative live snapshot: migrations 0014/0015/0016 executed on V2; audit_write_event live; protected counts as in §0.

### P0 findings — frontend inventory

- Stack: Vite 5 + React 18 SPA, tab-based navigation in `src/App.jsx` (no router). Components (19): Login, AdminHome (admin only), Dashboard, Tasks, Clients, Compliance, DocumentsHub, DocumentManager, WorkDocuments, Team, Usage, AuditLog (admin only), OnboardingWizard, AddTaskModal, FollowUpModal, HistoryModal, MarkFiledModal, ChatAgent, ErrorBoundary.
- Auth: Supabase auth session → fail-closed lookup in `team` (active member required); password-recovery flow present. Target binding `src/supabase.js`: env-only, no hard-coded ref, no V1 fallback (IR-1A).
- **Onboarding today:** OnboardingWizard writes `clients` directly (insert / update **keyed by `client_id` business code**, not `clients.id`), persists directors as `clients.directors` JSONB, stores `aadhaar_last4`/`aadhaar_masked` derived fields (full Aadhaar never persisted — enforced by `src/lib/aadhaar.js` + CI-tested), uploads to `documents`. Draft state supported. Compliance run is a separate explicit action via `complianceRunner.js`.
- **Direct table writes (complete inventory):** documents (DocumentManager, DocumentsHub, Compliance, MarkFiledModal, OnboardingWizard), completed_documents (WorkDocuments), follow_ups + tasks (FollowUpModal, AddTaskModal, Tasks), clients (OnboardingWizard, Clients doc_pin), financials_tracker / extracted_document_data / client_financials (Compliance), compliance_calendar (concurrency-safe upsert in complianceRunner). **None of the 7 M1-A child tables is written by the frontend yet** — P1 bypass closure breaks nothing in the current UI.
- **RPCs invoked by frontend:** `generate_client_compliance`, `activate_accounting_service` (complianceRunner), `get_sensitive_audit_logs` (AuditLog).
- Deferred/TODO markers: AdminHome "awaiting review: DEFERRED — no review-status field exists". No other actionable TODO/FIXME in `src/`.

### P0 findings — database artifacts in repo (files, not live state)

- Migration files on disk: 0001–0011 (V1-parity foundation: enums, people/clients, work/documents, trackers, audit phase-4b, RLS, dependency closure, functions/RPCs incl. role helpers `is_active_user`/`is_admin_or_manager`/`get_app_role`, views, refined RLS, storage/edge DEFER), 0014 pair (executed/closed), 0015 pair (executed/closed), 0016 pair (executed/closed), plus out-of-band 0017 (B-2). **Numbering note (corrected at housekeeping-2 from the 0014 header): 0012 = secure-docs, already executed live (no file in repo); 0013 = RESERVED for R3 (client-ID sequence), unused.** Neither number is free — do not reuse; the missing 0012 file is a corpus gap to flag at the next DB gate.
- M1-A tables (0015): entity_type_catalogue + client_persons, client_registrations, gst_registration_details, client_identifiers, client_contacts, client_addresses, client_relationships, client_remediation_flags — all with `row_version integer NOT NULL DEFAULT 1` (the live optimistic-lock mechanism), RLS enabled+forced, `REVOKE ALL FROM PUBLIC, anon`, `GRANT SELECT, INSERT, UPDATE TO authenticated` (direct DML still granted → to be revoked by D2b bypass closure), no DELETE grant.
- 0016 additions: `public.audit_write_event`, 6 family events, lineage columns (source_system, source_ref, source_hash, backfill_batch_id) + remediation rule columns (rule_code, rule_version, batch_id) + 2 indexes.
- Verification kits committed/staged: M1A + M1B_D2a read-only verification SQL.

### P0 findings — tests, build, CI

- `npm test`: **167/167 pass** (aadhaar, compliance, complianceRunner, complianceTabs, financialYear suites; includes static guards: no hard-coded FY, no full-Aadhaar persistence, coverage-gap fail-closed behaviour).
- `npm run build`: **passes** (vite 5.4.21, 101 modules, 3.4 s).
- CI: `.github/workflows/ci.yml` (npm ci → test → build → diff hygiene) + `pr-safety-gate.yml` (PR-body check + AI review). No lint gate yet (deliberate, documented).

---

## Gate register

### P1 — M1-B D2b secure audited CRUD (backend)
- **Workstream:** Module 1 backend. **Objective:** 19-ish hardened audited RPCs for the 7 M1-A child tables + mandatory bypass closure (revoke direct authenticated INSERT/UPDATE).
- **Scope:** per master instruction (SECURITY DEFINER, pinned search_path, null-uid rejection, null-safe `is_active_user()`/`is_admin_or_manager()` gates, clients.id UUID, validation, no Aadhaar, optimistic locking on `row_version`, atomic `audit_write_event`, error taxonomy not-found/conflict/validation/authorization, REVOKE PUBLIC+anon).
- **Dependencies:** P0 review closed; disposition of out-of-band 0017 (B-2); D2a baseline (closed). Execution additionally requires a safe V2-only execution channel (B-1).
- **Database objects:** functions on client_persons, client_identifiers, client_contacts, client_addresses, client_relationships, client_registrations, gst_registration_details; grants on those 7 tables.
- **Audit events:** person/identifier/contact/address/relationship/gst_detail `.changed`, `registration.updated`; actions CREATE/UPDATE; change_type_code CREATED/UPDATED/ENABLED/DISABLED; combined header+GST update emits both events atomically.
- **Frontend files:** none.
- **Expected files:** `supabase/migrations/0017_m1b_d2b_client_master_crud_rpcs.sql` + `_rollback.sql`, `supabase/verification/M1B_D2b_post_execution_verification_readonly.sql`, `docs/M1B_D2b_Implementation_Report.md`, test plan (unit/negative/concurrency), register update. Hashes + parser results + git evidence per Rule 11.
- **Risks:** R-2 (out-of-band 0017), R-1 (no safe execution channel), R-6 (Aadhaar last4 rule tension — RPCs must reject Aadhaar fields).
- **Acceptance criteria:** all master-instruction RPC properties verified by read-only SQL; protected counts unchanged; no unaudited write path to the 7 tables; rollback proven paired.
- **Status:** **D2b — CLOSED PASS** (2026-07-18; independently reviewed + executed on V2/yav2-dev; includes the follow-on 0018 DELETE privilege closure). Five artifacts: 0017 migration (22 RPCs incl. registration create/create_with_gst/set_active; G1 Aadhaar rejection; P11/P13–P16 validation; bypass closure; RAISE `%%`→`%` fix L154) sha `91c605a9…`; rollback (fail-closed) sha `3e43fbac…`; read-only verification (V1–V9 incl. coverage matrix, protected counts, no-D3/D4) sha `c402174f…`; transactional test kit (36 tests, always-ROLLBACK) sha `95e161e0…`; implementation report (matrix + AQ-1..7). Committed at `83339e539321cbc6c240b2b0f20f2e8b443f923d` on `ui/redesign-v1` (repository closure). **Final rulings applied 2026-07-18:** AQ-2 (P15 reg_type = IN_GST/AE_VAT/AE_CT/LICENCE/OTHER, OTHER extensible), AQ-3 (P16 jurisdiction = trimmed non-empty text ≤64 chars, not two-letter), AQ-4 (Aadhaar: reject TYPES only, no length-only 12-digit value reject; test T16 repurposed) — CLOSED. **Execution status:** EXECUTED on V2/yav2-dev — 0017 (RPCs + INSERT/UPDATE bypass closure, SHA `91c605a9…`) and 0018 (DELETE privilege closure, SHA `bbef5d3e…`, rollback `3ffa1a6e…`, read-only verification `1e2e1fa2…`); independently verified (`all_7_insert_update_delete_denied_select_kept_expect_true = true`). **Review:** CLOSED PASS (independent). **OBS-D2B-V9-1** (post-execution verification V9) preserved as **NON-BLOCKING** — does not gate closure. Open items AQ-1, AQ-5, AQ-6, AQ-7 are non-blocking deferrals to later phases. **% :** authoring 100, closure/gate 100 — CLOSED PASS. **Deviation D-P1-1:** concurrent session re-staged D2b files mid-authoring (07:51:56 IST), authored an unauthorized duplicate test kit (`M1B_D2b_functional_test_kit_transactional.sql`), and rewrote the report (corrected in place); dispositions recommended in report §H. **Next action:** D2b repository closure complete; await separate approval before starting the next phase.

### P2 — Client onboarding frontend (Preview)
- **Objective:** full Client Master UI (sections A–G) writing only via D2b RPCs; pilot mode for 2–3 real clients.
- **Dependencies:** P1 EXECUTED + VERIFIED. **Key discovery inputs:** current wizard keys writes on `clients.client_id` (must move to `clients.id`); current direct `clients`/`documents` writes must be re-routed or explicitly retained per approved scope; draft mechanism exists; Aadhaar capture UI still collects full number transiently to derive masked/last4 — **must be removed to meet the no-last-four rule** (R-6 decision).
- **Frontend files expected:** OnboardingWizard.jsx (major), Clients.jsx, new section components, lib validators; tests.
- **Deliverables:** tests, build, CI, screenshots, console + network evidence (RPC-only writes), Preview URL.
- **Overall P2 status:** **IN PROGRESS** — P2.1 CLOSED / PASS; P2.2 NOT STARTED.
- **Sub-phase status:**
  - **P2.1 — read-only Client Master Preview foundation: CLOSED / PASS** (2026-07-18). Admin/Manager-only, existing-client-only, keyed strictly on `clients.id` (uuid); read-only sections A/B/C/D/F/G plus deferred Relationships; no normalized-table write, no legacy dual-write, no `clients`-row creation, no compliance/tracker/calendar/FY, no D3/D4. Feature-flagged behind `VITE_P2_PREVIEW`.
    - **Repo commit:** `0aa6d63276ee3f898107cc7d31dcc1590e1b755c` (branch `ui/redesign-v1`; 13 files; 179 tests pass; vite build clean).
    - **Preview trigger commit:** `ff88c8da08b77a6ad58e80951455e8f452298efc` (empty commit; identical tree to `0aa6d63`), branch `ui/redesign-v1`.
    - **Preview deployments (both READY, target Preview, same commit `ff88c8d`):**
      a. **Original Git-linked deployment** `dpl_G9CGN1kT5ro3wo9FPRPuwaZjYj5E` — created **before** `VITE_P2_PREVIEW` was enabled for the Preview branch. (Not the deployment used for visible runtime verification.)
      b. **Subsequent Preview redeployment** `dpl_HAiz9RHEgu2yCHnQRM3nVWetYFuJ` (`action: redeploy`, `originalDeploymentId: dpl_G9CGN1kT5ro3wo9FPRPuwaZjYj5E`), URL `yes-advizors-portal-v2-preview-dqcuwf08k-yes-advizors-projects.vercel.app` — created **after** enabling `VITE_P2_PREVIEW` for branch `ui/redesign-v1`.
    - **`VITE_P2_PREVIEW` is enabled ONLY for the Preview branch `ui/redesign-v1`** (not in Production).
    - **Preview runtime verification was completed on the flag-enabled redeployment (b), `dpl_HAiz9RHEgu2yCHnQRM3nVWetYFuJ`** (Vercel, branch `ui/redesign-v1`): P2.1 Client Master Preview visible; Admin/Manager access works; correct client opens; sections A, B, C, D, F, G load; Relationships shows the deferred placeholder; all sections read-only; **zero records (expected — D3 backfill not started)**.
    - **Production is untouched** — the production deployment remains `dpl_5NhXDhficfZ1t2DY28NrZfYtUMGQ` (commit `ae6bf1e…`); `VITE_P2_PREVIEW` is NOT enabled in Production.
    - **Non-blocking observations (preserved):** **OBS-P2.1-1** — circular imports between the preview container and its section components; cleanup deferred unless a runtime failure occurs. **OBS-P2.1-2 (NON-BLOCKING, preserved)** — section isolation is mainly proven by static tests; runtime failure-path verification remains deferred. The successful runtime check proves normal loading and empty states only.
  - **P2.2 — write UI (normalized-table writes via D2b RPCs): NOT STARTED.**
  - **D3 (P3) legacy person backfill: CLOSED — NO MIGRATION REQUIRED / SAMPLE DATA** (2026-07-19). Discovery remains valid evidence of DB contents; the proposed 10-migrate / 16-HELD treatment is superseded after PJ confirmed all 26 `clients.directors` entries are sample/testing data. Migration 0019 was drafted locally, then **cancelled before execution** and **never committed**; 0 rows inserted into `client_persons`. (History below: discovery run 2026-07-18.)
  - **D4 (P4) remediation flags: DISCOVERY & DESIGN — PASS WITH NOTES** (2026-07-19; read-only discovery run by PJ on V2, no write). Population **deferred** behind a separate approval gate.

### P3 — D3 legacy person backfill
- **Objective:** backfill client_persons from `clients.directors` JSONB (11 clients, ~26 persons; client_directors legacy table 0 rows) with lineage columns, dedupe priority PAN→DIN→name+evidence, no auto-merge of ambiguous, batch-controlled, rerunnable, no JSONB deletion.
- **Rule tension R-6:** source JSONB rows carry `aadhaar_last4`/`aadhaar_masked`; master rule forbids copying digits/last-four → backfill must **drop** them (confirm at review).
- **Dependencies:** P1 (audit RPC layer), execution approval, safe execution channel (B-1). **Status:** **DISCOVERY EXECUTED — migration/backfill OPEN / BLOCKED** (2026-07-18). Discovery + design package: read-only V2 discovery SQL `supabase/verification/M1B_D3_legacy_person_discovery_readonly.sql` (9 all-SELECT blocks; counts, predicates and key names only; no Aadhaar value of any form emitted; Aadhaar counted-not-emitted) and design report `docs/M1B_D3_Discovery_And_Design_Report.md` (source→target mapping; **per-client** PAN→DIN→name+evidence dedupe with cross-client repeated identities preserved as separate associations; no auto-merge; lineage/batch_id; idempotency via the partial index `client_persons_source_uq` with explicit outcome buckets; `clients.directors` never mutated; no Aadhaar copy and no inferred Aadhaar verification).
  - **Read-only discovery EXECUTED on V2/yav2-dev (`ogjrwemjefvccpyjwxuo`) 2026-07-18** by the authorised executor (PJ) in the SQL Editor; 9 SELECT/CTE blocks, no write. Evidence recorded in report §B-Results (counts / key-names only — **no Aadhaar/PAN/name/mobile/email value recorded**). Headline results: **11 clients / 26 director entries**; **PAN = 0, DIN = 0 across all 26** (every entry weak-identity); **16 of 26 ambiguous** (name-only, no secondary signal), 10 with a secondary signal (not thereby migratable); **`client_directors` = 0 rows** and **`client_persons` = 0 rows**; **no source collision**; **`client_persons_source_uq` index present**; protected baseline captured (audit_log 20; clients 13/13; trackers 312/120/26; compliance_calendar 0; remediation 0; directors probe 26). **Corrected finding:** contrary to the earlier `expect_0` assumption, **24 of 26 legacy entries carry a raw `aadhaar` key** (count only; no value emitted; no digit may reach `client_persons`; `aadhaar_verification_status` stays neutral `'Not Provided'` if a backfill is ever approved).
  - **Migration/backfill 0019 remains OPEN / BLOCKED — NOT AUTHORED, NOT EXECUTABLE.** Three unresolved gates before any authoring: **(a) audit implementation choice** — audit provenance for a non-user migration context is an OPEN BLOCKER (no approved emitter works without `auth.uid()`; no service actor / user-UUID / bypass assumed); **(b) treatment of the 16 ambiguous records** — must not be represented as deterministically resolved identities (exclude / D4 remediation queue / hold — undecided), and the 10 secondary-signal entries are not automatically migratable; **(c) corrected handling of the 24 raw-`aadhaar`-key records** — the backfill must ignore the raw `aadhaar` key so no Aadhaar digit is copied, with verification status held neutral. Awaiting independent review + explicit approval before authoring.
  - **CLOSURE 2026-07-19 — D3 CLOSED: NO MIGRATION REQUIRED / SAMPLE DATA (supersedes the OPEN/BLOCKED status above).**
    - **Business fact:** PJ confirmed all 26 `clients.directors` JSONB entries are **sample/testing data** — not real legacy records — and disposable. No backfill/migration is required; the OPEN gates (a)/(b)/(c) above are moot.
    - **D3 discovery remains valid evidence of the database contents.** The proposed backfill treatment (later revised to a 10-migrate / 16-HELD design) was **superseded** after the sample-data confirmation; the discovery itself is not invalidated.
    - **Migration 0019 was cancelled before execution.** A draft package (2 drafts + 5 read-only verification SQL, incl. an HMAC identity-lock design) was authored locally under independent review but **never executed** and **never committed**. **No rows were inserted** into `client_persons`; **no database data was deleted or mutated**. A draft HMAC approval-manifest SQL file was prepared locally but never executed; no HMAC output manifest was generated or approved; **no secret was stored in the repository, documentation, committed history, or shared review output**.
    - **Disposition:** the never-committed 0019 SQL files were **deleted locally** (closure documentation is the sufficient historical evidence); the committed D3 discovery SQL + reports are preserved. Closure record: `docs/M1B_D3_0019_Draft_Decision_And_Mapping.md`.
    - **Sample-data removal is deferred** to a separately-reviewed **V2 Clean-Start Reset** (`docs/M1B_Future_V2_Clean_Start_Reset_Task.md`), to be authored/executed only after implementation + testing, immediately before real onboarding — **not now**.
    - **No D4 / P2.2 work was started** by this closure.

### P4 — D4 remediation flags
- **Objective:** **D4 is currently discovery/design only.** `MISSING_INCORP_DATE` is the **only confirmed first-pass remediation rule**. `LEGACY_JSONB_DIRECTORS` is a **temporary sample-cleanup observation only** — **not** a permanent remediation rule and **not authorized for population**. All other candidate observations remain **non-approved**. Any population of `client_remediation_flags` (rule_code/rule_version/batch_id, unresolved-uniqueness, audited resolution, remediation UI) requires a **separate approval gate and a governed write-path review** — none is authorized now.
- **Dependencies:** **P1** (audited write path). Legacy JSONB-director cleanup is **not** part of the D4 remediation-flag lifecycle — it belongs to the **separately approved future V2 Clean-Start Reset**, so D4 does **not** depend on P3. **Status:** **DISCOVERY & DESIGN — PASS WITH NOTES** (2026-07-19). Population NOT STARTED / deferred.
- **DISCOVERY & DESIGN 2026-07-19 — PASS WITH NOTES.** Package (documentation + read-only SQL, no write): design report `docs/M1B_D4_Discovery_And_Design_Report.md` + read-only discovery `supabase/verification/M1B_D4_remediation_discovery_readonly.sql` (aggregate counts + schema metadata only; every statement SELECT/WITH; three scopes with reconciliation; no UUIDs/personal values). **Read-only discovery executed by PJ on V2/yav2-dev — no database write, no remediation rows, no migration, no application-code change.**
  - **Evidence (counts only):** clients 13 (production-eligible 11, sample/test/draft/inactive 2; only 2 marked `is_test_client`). `client_remediation_flags` = 0 rows. Schema 10/10 columns; `client_remediation_flags_open_uq (client_id, rule_code) WHERE resolved=false` present; RLS enabled+forced. `MISSING_INCORP_DATE` = 11 all / 9 production-eligible / 2 sample (reconciled). `LEGACY_JSONB_DIRECTORS` = 11 clients / 26 director elements (production-eligible 11/26; sample 0/0; reconciled) — TEMPORARY SAMPLE-CLEANUP OBSERVATION, consistent with D3.
  - **Confirmed first-pass rule:** only **`MISSING_INCORP_DATE`** (design only, NOT populated). **`LEGACY_JSONB_DIRECTORS`** remains a **temporary sample-cleanup observation**, not a permanent rule.
  - **Candidate observations — NON-APPROVED (no severity/resolution/population authority):** malformed PAN 0; duplicate PAN groups 1 (production-eligible 1, mixed-scope 0); duplicate GSTIN/CIN groups 0; GST-registration mismatch 0; **unmapped entity type 10** (production-eligible 10). On PJ-confirmed **sample data** these are sample-data artifacts; **not** genuine production defects until the future V2 Clean-Start Reset + clean-data review.
  - **Security NOTE (no change made):** `anon` SELECT false; `authenticated` holds table SELECT/INSERT/UPDATE/DELETE grants; policies for SELECT/INSERT/UPDATE; **no DELETE policy** (RLS denies deletes). D4 changed **no** privilege/grant/RLS/policy/function. A governed remediation write path is unbuilt; the `%remediation%` name check (0 functions) does not prove absence of a differently-named writer — requires repository review.
  - **Deferral:** population phase remains **deferred** and requires a **separate approval gate**; the audited write-path + actor-provenance decision is unresolved. No D4 population, RPC, migration, or P2.2 work was started.

### P5 — Service applicability
- **Objective:** per-client service applicability layer (accounting, GST, TDS, payroll, income tax, ROC, LLP, statutory audit, tax audit, secretarial…) with effective/end dates, frequency, registration linkage, assignee/owner, controlled status, audit, optimistic locking; defines "Ready for Service Applicability" = Module 1 completion. **Records approved service applicability only — no compliance generation from Client Master edits.**
- **Discovery input:** a legacy `activate_accounting_service` RPC exists (0008/0014) — P5 must reconcile/supersede it. **Dependencies:** P2 (UI base), P1. Separate backend and frontend checkpoints.
- **Status:** **P5 backend schema/RPC foundation — EXECUTED / CLOSED PASS** on V2/yav2-dev (Migration 0021, 2026-07-19). Discovery & design remains CLOSED PASS WITH NOTES. **P5 UI read-only checkpoint CP-4 (read-only Service Applicability section) — CLOSED PASS** (2026-07-20, corrected local runtime verification; commit `f9f2eb3989811c50c32fec65be78f2df41281721`; see P5-UI CP-4 closure record below). **PG-1 backend correction (Migration 0022 — authoritative OTHER-notes enforcement) — EXECUTED / CLOSED PASS** on V2/yav2-dev (2026-07-21; committed `6a96b3d5bd7203daf2c421169ca0261a8435b505`; see P5 PG-1 record below). **P5 UI write checkpoints CP-5 / CP-6 / CP-7 — IMPLEMENTED / CORRECTED · PJ LIVE RUNTIME VERIFICATION COMPLETE (PASS, 2026-07-21 IST)** (see the P5 UI CP-5/6/7 record below). **P5 and Module 1 are CLOSED PASS — Independent ChatGPT review PASS; PJ final approval granted 2026-07-21 IST.**
- **DISCOVERY & DESIGN 2026-07-19 — PASS WITH NOTES.** Package (documentation + read-only SQL, no write): design report `docs/M1B_P5_Service_Applicability_Discovery_And_Design_Report.md` + read-only discovery `supabase/verification/M1B_P5_service_applicability_discovery_readonly.sql` (aggregate counts + catalogue/schema metadata only; every statement SELECT/WITH; three-scope legacy-services counts; no UUIDs/personal values). **Repository + live read-only discovery (7 blocks) executed by PJ on V2/yav2-dev — no database write, no migration, no application/database object change.**
  - **Historical pre-0021 discovery snapshot (superseded by the execution closure below):** normalized `service_catalogue` and `client_service_applicability` layers **do not yet exist**; **no service-applicability RPC exists** (0 `%applicab%`); legacy `activate_accounting_service` present and **two `generate_client_compliance` functions found by name** (name-count only; overload signatures not proven) — **legacy compliance-coupled generators, SUPERSEDED / must not be reused**. `clients` 13 rows; **`clients.id` uuid remains the authoritative client key**; legacy `clients.services` jsonb: **2 clients have non-empty services (3 total elements); both are technically production-eligible under the discovery predicate and 0 rows fall in the sample/test/draft/inactive scope** — note the DB sample/test flags do NOT classify these two rows as sample; **PJ has separately confirmed the current dataset is sample/testing data**, so these are treated as **legacy sample observations, not genuine production applicability**. `team` 7 (active 6). **`client_registrations` currently has 0 rows.** Reuse infra present (`entity_type_catalogue`, `is_active_user`, `is_admin_or_manager`, `audit_write_event`, `financial_years`, `gen_random_uuid`); **`client_registrations` has no `UNIQUE (id, client_id)`** → the future **composite same-client FK requires an additive `UNIQUE (id, client_id)`**. **Post-0021 (current state after execution, per the closure below):** `service_catalogue` **now exists**; `client_service_applicability` **now exists and is empty (0 rows)**; **all three applicability RPCs now exist**; **`client_registrations_id_client_uq` now exists**. (The snapshot figures in this bullet are the pre-0021 discovery numbers and are left unchanged as historical evidence.)
  - **PJ-approved design decisions (documented in the report §4):** service catalogue `ACCOUNTING, GST, TDS, PAYROLL, INCOME_TAX, ROC, LLP, STATUTORY_AUDIT, TAX_AUDIT, SECRETARIAL, OTHER` (ROC/LLP separate; STATUTORY_AUDIT/TAX_AUDIT separate); frequency vocabulary `MONTHLY, QUARTERLY, HALF_YEARLY, ANNUAL, EVENT_BASED, ONE_TIME, AS_REQUIRED`; lifecycle = single authoritative `status` (Draft/Approved/Inactive); `effective_from` NULL-in-Draft but mandatory before Approved; history = stop→Inactive+`effective_to`, restart→new row (no reopen); registration linkage mandatory only where `requires_registration=true`, same-client **DB-enforced** via composite FK `ON DELETE RESTRICT`; owner = `team(id)`; actors `created_by/updated_by/approved_by = auth.uid()` (approval system-controlled via governed RPC); legacy `clients.services` disposition deferred to the future V2 Clean-Start Reset; `OTHER` admin-controlled with mandatory notes + periodic promotion review.
  - **Boundary (reconfirmed):** Module 1 reaches **"Ready for Service Applicability" at the approved design boundary only — NOT at service/compliance generation.** No compliance tracker rows, FY obligations, due dates, overdue status, or calendar entries may be generated by Module 1. Legacy `activate_accounting_service` / `generate_client_compliance` remain superseded/not reused.
  - **Historical implementation gate before execution** *(pre-0021 authorization boundary, retained verbatim as the design record; this backend migration gate was **subsequently authorized and closed** by the execution-closure record below)*: at design time the **P5 backend schema/RPC foundation** was **NOT STARTED and required a separate migration/RPC approval gate**. **Migration `0021`, if separately authorized, is a schema-and-controlled-reference-data migration ONLY.** It **must NOT**: migrate, backfill, copy, delete or modify `clients.services`; create `client_service_applicability` rows from existing clients; perform any client-data cleanup; or treat current sample/testing data as production data. **Permitted future scope (only after separate approval)** is limited to creating tables (`service_catalogue`, `client_service_applicability`), constraints, indexes, RLS, policies and audited RPCs (incl. the additive `UNIQUE (id, client_id)` on `client_registrations`), and **seeding only the PJ-approved `service_catalogue` reference codes** (no client rows). Legacy `clients.services` disposition remains **deferred to the separate V2 Clean-Start Reset**. No P2.2, D4 population, compliance generation, or Clean-Start Reset started by this closure.
  - **EXECUTION CLOSURE 2026-07-19 — Migration 0021 EXECUTED / CLOSED PASS on V2/yav2-dev (`ogjrwemjefvccpyjwxuo`).** PJ manually executed `supabase/migrations/0021_service_applicability.sql` once in the V2 SQL Editor (single transaction; COMMIT; no visible error). **Pre-checks:** 9/9 base tables, 3/3 helpers, all 0021 objects/events/`client_registrations_id_client_uq` absent, `PASS_pre_execution_ready=true`; PRE-6 baseline clients 13 · clients.services elems 3 · client_registrations 0 · audit_log 20 · audit_event_contract 20 · accounting_tracker 312 · financials_tracker 120 · income_tax_tracker 26 · compliance_calendar 0 · clients.id uuid. **Post-checks V1–V8 all PASS:** service_catalogue 11 rows / 11 exact codes; `client_service_applicability` 0 rows; `PASS_v3_constraints=true` (four named business CHECKs + status CHECK + composite same-client FK + ON DELETE RESTRICT + `client_registrations_id_client_uq` + live partial unique); RLS enabled+forced on both new tables; catalogue authenticated SELECT-only; applicability authenticated SELECT-only (no direct DML; anon denied); 3 RPCs SECURITY DEFINER, EXECUTE authenticated-only (anon/service_role/PUBLIC denied); 4 audit events (added→CREATE; approved/deactivated/updated→UPDATE; MEDIUM/S2; resource `client_service_applicability`). **V8 vs PRE-6:** audit_event_contract **24** (+4), audit_log **20** (unchanged), service_catalogue **11**; clients 13, clients.services elems 3, client_registrations 0, trackers 312/120/26, compliance_calendar 0, applicability 0 — all unchanged. **No applicability rows populated; no compliance/tracker/calendar rows generated; no `clients.services` change; V1/Production untouched.** **Migration must NOT be rerun; rollback NOT authorized** (separate PJ+ChatGPT decision only). This closes only the **P5 backend schema/RPC foundation — EXECUTED / CLOSED PASS**. At the time of the Migration 0021 execution closure on 2026-07-19, the P5 UI and client-applicability workflow remained unopened and unauthorised. Subsequently, read-only UI checkpoints CP-1 through CP-4 were implemented, and CP-4 was CLOSED PASS on 2026-07-20 as recorded below. PG-1 (Migration 0022) was subsequently AUTHORED, independently reviewed, committed, and EXECUTED / CLOSED PASS on V2/yav2-dev on 2026-07-21 (see the P5 PG-1 record below). CP-5 is now unblocked for separate authorisation but has not started; CP-6 remains NOT STARTED / NOT AUTHORISED. Evidence: `docs/M1B_P5_0021_Execution_Evidence.md`. Governing commits: migration `05796268…`, execution-readiness `814a52b6…`.
  - **P5 UI CP-4 CLOSURE 2026-07-20 — read-only Service Applicability UI section CLOSED PASS.** Documentation-only closure of implementation checkpoint **CP-4** (`docs/M1B_P5_UI_Discovery_Design_And_Implementation_Plan.md` §14), following **corrected local runtime verification** approved by PJ. **Commit:** `f9f2eb3989811c50c32fec65be78f2df41281721` (branch `ui/redesign-v1`, in sync with origin) — `feat(p5): add read-only service applicability UI`; component `src/components/serviceApplicability/ServiceApplicabilitySection.jsx` mounted in `src/components/preview/ClientMasterPreview.jsx` after Relationships. **Test/build:** service-applicability `node --test` suite **57/57 pass**. **Corrected runtime verification procedure:** local Vite dev server started with **process-scoped** flags `VITE_P2_PREVIEW=true` and `VITE_P5_UI=true` (no `.env.local` or any file change) at `http://localhost:5173/`; logged in as an active Admin (existing session); Clients → open a client detail → the **"🧩 Client Master (Preview)"** button appeared (proves the `VITE_P2_PREVIEW` gate) → the correct **read-only Client Master Preview** modal opened → **Service Applicability rendered after Relationships** (proves the `VITE_P5_UI` gate). **Verified results:** live **empty state** ("No active service applicability records are configured for this client.") for **YA-008** and **YA-011**; **Refresh** completed cleanly (re-read, no error); **read-only confirmed** — only a Refresh control, **no** create/edit/delete/approve/activate/deactivate controls; **no browser-console, React, or Supabase read errors**. **Earlier (2026-07-20) not-visible observation — root cause, now resolved:** the section lives only inside the `VITE_P2_PREVIEW`-gated Client Master Preview modal; the earlier check set only `VITE_P5_UI` and viewed the normal client-detail page whose final section is Documents. **Non-blocking residuals (do NOT block CP-4):** populated active-record and history/inactive runtime states were unavailable (no existing rows in V2; creating data is out of scope) and the non-Admin hidden-state was not runtime-exercised — all three remain covered by the passing unit tests. **Governance:** documentation-only; **no** source/test/SQL/migration/env/config change; **no** Supabase MCP, **no** SQL, **no** database mutation; V1/Production untouched; not committed/pushed by this step. **CP-5 remains NOT AUTHORISED** (gated on the PG-1 backend correction being EXECUTED / CLOSED PASS on V2/yav2-dev).
  - **P5 PG-1 (Migration 0022) — EXECUTED / CLOSED PASS (2026-07-21).** Prerequisite backend correction before CP-5: authoritative enforcement that an `OTHER` service applicability row must carry meaningful (non-blank) notes. **Two-layer design (PJ-approved):** (1) table CHECK `csa_other_notes_required_chk` = `service_code <> 'OTHER' OR (notes IS NOT NULL AND btrim(notes) <> '')` — valid single-row same-table CHECK because `service_code` is stored on the row; (2) controlled guards in the create + update RPCs raising `OTHER_NOTES_REQUIRED` (already mapped in `src/lib/serviceApplicabilityErrors.js`). `service_applicability_set_status`, audit-event contracts, RLS, grants, service catalogue and all frontend/source are **unchanged**. **Migration 0022** is additive, single-transaction, fail-closed, not rerunnable, and authorised only on an **empty** applicability table (0 rows). Tentative **P6** compliance-generation migration moves to **0023** (numbering table updated). **Package (committed at `6a96b3d`; Migration 0022 executed once on V2, then CLOSED):** `supabase/migrations/0022_p5_pg1_other_notes_enforcement.sql`; `supabase/verification/rollback_0022_p5_pg1_other_notes_manual.sql` (separate, manual, conditional — fails only if an OTHER row has blank notes — requires separate PJ+ChatGPT approval; warns it weakens enforcement); `supabase/verification/M1B_P5_PG1_pre_execution_verification_readonly.sql`; `..._post_execution_verification_readonly.sql`; `..._transactional_functional_tests.sql` (rollback-only); `docs/M1B_P5_PG1_Implementation_Plan.md`; `docs/M1B_P5_PG1_Execution_Evidence.md`. **Review + commit:** ChatGPT PASS FOR COMMIT; committed at `6a96b3d5bd7203daf2c421169ca0261a8435b505` (branch `ui/redesign-v1`). **EXECUTION CLOSURE 2026-07-21:** PJ executed Migration 0022 once on V2/yav2-dev (`ogjrwemjefvccpyjwxuo`) — pre-execution **14/14 PASS**, post-execution **15/15 PASS**, PG1_POST_BASELINE matched PG1_PRE_BASELINE exactly (applicability 0 · catalogue 11 · clients 13 · clients.services elems 3 · registrations 0 · audit_log 22 · audit_contract 24 · accounting_tracker 312 · financials_tracker 120 · income_tax_tracker 26 · compliance_calendar 0); constraint `csa_other_notes_required_chk` present with the intended definition (`service_code <> 'OTHER' OR (notes IS NOT NULL AND btrim(notes) <> '')`); create + update guards present; `set_status`, RLS (enabled+forced), grants, catalogue and audit-contract unchanged; `authenticated` EXECUTE retained while anon/service_role/PUBLIC denied; applicability table remained empty; a **second run was blocked by the rerun guard** (`csa_other_notes_required_chk` already existed); transactional tests PASS (final post-rollback applicability 0, pass=true). **Migration 0022 must NOT be rerun; rollback remains UNAUTHORISED and was not executed; V1/Production untouched.** Evidence: `docs/M1B_P5_PG1_Execution_Evidence.md`. **CP-5 is now UNBLOCKED for separate authorisation but has NOT started (nor CP-6/P6).**
  - **P5 UI CP-5 / CP-6 / CP-7 — IMPLEMENTED / CORRECTED · PJ LIVE RUNTIME VERIFICATION COMPLETE (PASS, 2026-07-21 IST) · P5 and Module 1 CLOSED PASS (Independent ChatGPT review PASS; PJ final approval granted 2026-07-21 IST).** *(Supersedes the "CP-5 not started" notes above; this is a narrowly-scoped P5 UI status entry — it does NOT rewrite or reopen PG-1, whose committed closure/evidence at commit `a2e11777…` is untouched.)* The frontend write UI on the closed PG-1 (0022) backend: create/edit Draft, approve, deactivate, "Start again" (restart → new Draft), capability-gated row actions, bottom-centre toast, optimistic-lock conflict UX (close + refresh + require reopen). **All writes flow ONLY through the CP-2 service wrappers → 0021/0022 RPCs** (no direct Supabase/`.rpc`/`.from` in the UI); validators/builders are the CP-1 pure logic; the DB (incl. PG-1 OTHER-notes + `row_version` guards) remains the sole write authority. `effective_to` is collected only when deactivating; Inactive rows are never reopened; no delete UI. **Rev 2 corrections (ChatGPT PASS WITH SPECIFIC CORRECTIONS):** WAI-ARIA modal focus management (initial focus, Tab/Shift+Tab trap, focus restoration, `tabIndex={-1}` panel) + tests; safe async write handling (`try/catch/finally`, `busy` always resets, thrown errors via `mapRpcError`) + tests; documentation wording. **Files:** added `ServiceApplicabilityModalShell.jsx`, `ServiceApplicabilityFormModal.jsx`, `ServiceApplicabilityStatusModal.jsx`; modified `ServiceApplicabilitySection.jsx`, `ServiceApplicabilityLiveTable.jsx`, `ServiceApplicabilityHistory.jsx`; tests `serviceApplicabilityUI.test.js` (updated) + `serviceApplicabilityWriteUI.test.js` (new, 17 tests); docs `M1B_P5_UI_CP5_CP6_CP7_Closure.md` + this register + UI plan §14. **Verification:** ✅ automated/static (`node --test` **322/322**; `vite build` clean); ✅ non-mutating runtime smoke (Add button + form modal + client-side validation; no RPC/DB write; no console errors); ✅ **LIVE runtime verification by PJ COMPLETE — PASS (2026-07-21 IST)** on the immutable Preview linked to governing HEAD `3a5f439c15cafa493cd2d2320d7733f286441b6b` (`https://yes-advizors-portal-v2-preview-f90l6sj2s-yes-advizors-projects.vercel.app`, deployment `dpl_8y7CXhTpeFYAZiknWUJFGaf2Md2v`, Preview/READY/git, `githubCommitSha=3a5f439…`) against V2/yav2-dev. **Steps 1–14 PASS:** **Step 12** non-Admin role-gating — logged in as `info@yesadvizors.com` (Role `Staff`; UID `94187bf5-d163-4763-adc4-ebfa52871686`; `team` row `9eccb3d4-1e55-4fae-a896-5b7e79165a25`; `P5 Non-Admin Test User`; active/not-admin/`Staff`); login OK; on Clients Onboarding the separate **"Client Master (Preview)"** button was **hidden** for the Staff user, so the **Service Applicability section and all its write actions were inaccessible** (the Staff user did **not** open Service Applicability — it was unreachable); the normal Client Record modal remained available; Firm Overview + Audit Log hidden — **fail-closed non-Admin gating confirmed**; **Step 13** no side effects — `accounting_tracker` 312 · `financials_tracker` 120 · `income_tax_tracker` 26 · `compliance_calendar` 0 (baseline exact); **Step 14** clean — Admin & non-Admin `v_firm_dashboard` returned 200, no red network request, Console clean. The earlier Step-14 `due_soon` **42703** was an investigated + resolved **stale-deployment / incorrect-URL** issue (clean domain served older bundle `index-I7iIBOgp.js`); HEAD already selects `due_in_7_days`; **no database view change and no source change were required**. **Residual notes:** the `Staff` test-user row is a **deliberate P5 runtime-verification record**, **remains active**, removal/deactivation **deferred to a future explicit PJ decision** (not done now); the Chrome "Dangerous" label on the `*.vercel.app` host is a **separate Safe-Browsing concern, not a P5 functional failure** (not claimed resolved). **No new migration/SQL, no Supabase connection, no compliance/tracker/calendar generation, no P6, no V1/Production, no Production deploy/merge.** **P5 and Module 1 are CLOSED PASS — Independent ChatGPT review PASS; PJ final approval granted 2026-07-21 IST.** Evidence: `docs/M1B_P5_UI_Runtime_Verification_Interim_Evidence.md`, `docs/M1B_P5_Runtime_Verification_Closure_Summary.md`, `docs/M1B_P5_UI_CP5_CP6_CP7_Closure.md`. Closure documents committed to `ui/redesign-v1`.

### P6 — Controlled compliance generation
- **Objective:** idempotent, FY-aware generation from approved service applicability; dry-run first; lineage (generated_by/at); safe re-run; concurrency-protected; 0014 untouched.
- **Discovery input:** existing paths = `generate_client_compliance` RPC + `complianceRunner.js` upsert (`onConflict client_id,compliance_tracker_id, ignoreDuplicates`) — to be superseded/gated by applicability-driven generation. **Dependencies:** P5 CLOSED. **Status:** NOT STARTED.
- **P6 SELECT-only live diagnosis — COMPLETED (documentation-only); verdict PASS WITH SPECIFIC CORRECTIONS.** **42/42** read-only blocks executed by PJ on V2/yav2-dev (`ogjrwemjefvccpyjwxuo`); **no mutation performed** (no DDL/DML/migration/function change). **Backend FY 2026-27 support VERIFIED LIVE:** `financial_years` holds FY 2022-23→2030-31; single current FY = 2026-27; `get_current_fy()` = 2026-27; `generate_client_compliance_core()`/`activate_accounting_service()` generate through `get_current_fy()`; **no reviewed function caps at `<= '2025-26'`** (the 2025-26 references are start-policy fallback only). The "FY 2025-26" warning is therefore inferred **stale frontend** wording/logic, not a backend cap. **Application migration ledger (`supabase_migrations.schema_migrations`) was NOT independently visible** — only `auth/realtime/storage` subsystem tables were; **no migration is asserted as applied from live behaviour alone.** A **13-item correction register** (stale FE FY warning; missing Service-Applicability enforcement; incomplete later-of start rule; YA-012 missing dates/applicability; broad table grants; no FORCE RLS; role-wide policies; Staff UPDATE inconsistencies; definer `search_path` hardening; missing `notice_tracker`/`trust_ngo_tracker` unique keys; payroll registration/establishment gap; soft refs without FK; migration-ledger visibility) and package recommendations **P6A–P6D** are captured in `docs/M1B_P6_SELECT_Only_Diagnosis_Closure_Report.md`. **Implementation/remediation remains UNAUTHORISED. Status: NOT STARTED (diagnosis complete; remediation not authorised).**
- **P6 pre-implementation design corpus — PRESERVED ON GITHUB — REMOTE HASH VERIFIED (design / PROPOSED evidence only).** A **13-file P6 design corpus** authored 2026-07-21/22 IST (`docs/M1B_P6_*.md`: Repository & Architecture Discovery Report; Business Rules & Data-Flow Design; Data Model & Generation Architecture; UI & Role Workflow Design; Security & Test Matrix; Implementation Readiness & Execution Plan; Review Checklist; Continuation Handshake Notes; Discovery Result Capture Template; PJ Decision Register; PJ Due-Date Decision Sheet; Service Due-Date Matrix — Rev10 PROPOSED DRAFT; Statutory Source Evidence Register), plus the read-only execution capture `docs/YAV2_P6_Readonly_Discovery_Execution_Capture_22_July_2026_IST.md` and **two SELECT-only discovery/diagnosis kits** under `supabase/verification/` (`M1B_P6_discovery_readonly.sql` and `M1B_P6_final_select_only_v2_diagnosis.sql` — the 42-block kit, transferred from `artifacts/`), was **committed and pushed to `origin/sync/integration`** in **preservation commit `de3c341a6571dddaec0f7c482a6edc5550850cbf`** (2026-07-26); the **remote-tracking hash was verified equal to the local commit**. **These 16 files are therefore no longer unique local-only records.** The local source copies in the recovery checkout may be removed **only under the separately authorised cleanup package** (not authorised here). **Nature: design / PROPOSED evidence only — nothing implemented.** PJ framework decisions **D-01 / D-06 / D-12 are APPROVED (2026-07-22 IST)**; **open PJ due-date decisions remain OPEN** and the Service Due-Date Matrix stays a PROPOSED DRAFT (11 rows on HOLD, not for approval). **No tentative migration number is approved** — `0023`/`0024` are already consumed by the `remediation-t4` package, so any P6 migration number must be reconciled against GitHub, this register, `remediation-t4` and live evidence before authoring (do not assume/reuse `0023`/`0024`). **P6 diagnosis is COMPLETED; P6 implementation/remediation remains NOT STARTED and UNAUTHORISED.**

### P7 — Tasks and recurring operations
- **Discovery input:** Tasks/AddTaskModal/FollowUpModal exist with direct table writes; no recurrence engine found. **Dependencies:** P5 (linkage), P9 (roles) partial. **Status:** NOT STARTED.

### P8 — Document management
- **Discovery input:** DocumentsHub/DocumentManager/WorkDocuments exist (direct writes, doc_pin gate, visibility toggle, deletes are hard `delete()` — archive policy needed); storage/edge migration 0011 was DEFERred. **Status:** NOT STARTED.

### P9 — Team, RBAC and masked reads (M1-C)
- **Discovery input:** role helpers exist (get_app_role, is_admin/is_admin_or_manager, get_portal_role); UI gates AdminHome/AuditLog on `is_admin`; Team.jsx exists; no masked-read layer yet. **Status:** NOT STARTED.

### P10 — Dashboard and reporting
- **Discovery input:** Dashboard + AdminHome exist; client counts previously corrected (4317388); "awaiting review" metric deferred (no field). Truthfulness audit required against approved sources. **Status:** NOT STARTED.

### P11 — WhatsApp and automation
- **Discovery input:** no WhatsApp/n8n code in the governed repo (ChatAgent.jsx is an in-portal agent UI; Usage.jsx tracks API usage). Existing bot/n8n work lives outside this repository — inventory it at gate start; do not re-enable. Execution requires separate approval. **Status:** NOT STARTED.

### P12 — End-to-end UAT and Preview closure
- **Input:** YAV2_Readiness_Package_Rev1.5 (UAT/SOP, committed at repo root) as seed material. **Status:** NOT STARTED.

### P13 — Production release package (no merge, no deploy)
- **Status:** NOT STARTED.

---

## Dependency map

```
P0 ─▶ P1 ─▶ P2 ─▶ P5(FE) ─▶ P6 ─▶ P7 ─▶ P12 ─▶ P13
      │      ▲     ▲
      ├─▶ P3 ─▶ P4 ┘ (P4 UI needs P2 shell; P5 backend needs P1 only)
      P8, P9, P10 — parallel after P2 shell; P9 gates P12
      P11 — independent plan, separate approval
B-1 (V2 execution channel) gates every EXECUTED state from P1 onward.
```

## Post-Module-1 Security Hardening (T4 — branch `sync/integration`)

Function-privilege and `search_path` least-privilege hardening on **V2/yav2-dev (`ogjrwemjefvccpyjwxuo`)**, executed manually by PJ, post-verified and runtime-verified.

> **Advisory (non-risk, numbering only):** these are **NOT** formal `supabase/migrations/` files — the SQL lives in `supabase/verification/remediation-t4/` and is named `0023_grants_hardening` / `0024_search_path_hardening`. Those names are local to that folder and are **distinct from** the reserved formal migration numbers `0023` (P6 compliance generation) / `0024+` (P7–P9) in the numbering table below, which are **unchanged**. No formal migration numbering is altered by this section.

- **G-05 / V-5 — CLOSED PASS** (grant hardening, `remediation-t4/0023_grants_hardening`, executed on V2). EXECUTE on the 17 functions removed from PUBLIC/anon/service_role — Group A (6 internal audit helpers) owner-only; Group B (11 role/calc/sensitive) `authenticated` retained. POST-verified; admin/non-admin runtime verified (audit-log allow / non-admin `42501` deny). Committed evidence: `supabase/verification/remediation-t4/POST_EXECUTION_RESULTS_0023.md`, `EXECUTION_CLOSEOUT_0023.md`. **% : authoring 100, closure/gate 100 — CLOSED PASS.**
- **V-4 — CLOSED PASS** (search_path hardening, `remediation-t4/0024_search_path_hardening`, executed 2026-07-26 on V2/yav2-dev). The 3 SECURITY DEFINER helpers `get_portal_role()`, `is_active_user()`, `is_admin_or_manager()` repinned from bare `search_path=public` to `pg_catalog, public, pg_temp`. **Evidence:** **PRE-4a PASS** (3 = `{search_path=public}`); **PRE-4b PASS** (public-schema CREATE — PUBLIC/anon/authenticated = **false**, shadowing precondition absent → defence-in-depth); **migration executed successfully in one transaction on V2/yav2-dev**; **POST-4 PASS** (3 = `{search_path=pg_catalog, public, pg_temp}`); **non-admin runtime PASS** — restricted client onboarding **denied server-side `42501`**, **no client or compliance records created**; **admin runtime PASS** — client **YA-012** created, compliance records generated, **no `42501`/permission error**. No function body / ownership / grants / schema / data change. Committed evidence: `supabase/verification/remediation-t4/0024-execution-readiness/POST_EXECUTION_RESULTS_0024.md`, `EXECUTION_CLOSEOUT_0024.md`. **% : authoring 100, closure/gate 100 — CLOSED PASS.**
- **Combined effect (scoped to V-4):** the scoped V-4 hardening of `get_portal_role()`, `is_active_user()` and `is_admin_or_manager()` is complete on V2 (alongside the G-05/V-5 grant least-privilege). **This closure does not establish that every SECURITY DEFINER function in `public` has the hardened `pg_catalog, public, pg_temp` search_path.** The later P6 diagnosis identified additional generator functions using `public, pg_temp`; their hardening remains **proposed under P6C**. The earlier closed V-4 package is not reopened; only this overbroad wording is corrected.
- **Overall-project completion:** this register tracks **per-package status + authoring/closure/gate %** and does **not** maintain a single weighted overall-project percentage. The separate T4 recovery governing model records the weighted overall figure at **≈ 42.9% before → ≈ 43.4% after** V-4/0024 (**+0.5 pp**; Area 4 Security +0.30, Area 7 Runtime +0.20) in `EXECUTION_CLOSEOUT_0024.md`; **that weighting is neither re-derived nor changed here.**
- **Governance:** V2/yav2-dev only; V1/Production never touched; no application/source change; no deployment/PR/merge. The earlier FY 2026-27 backend-generation-limitation hypothesis has since been **SUPERSEDED by the P6 live diagnosis** (backend FY 2026-27 support VERIFIED LIVE); the remaining open item is a **frontend warning/logic correction** — see **R-11**. G-11 (Team Workload) is now **CLOSED PASS** — see the G-11 section below and **R-10**.

## Post-Module-1 UI defect closure (G-11 — branch `sync/integration`)

Removal of the broken `v_team_workload` dependency and always-empty "Team Workload" panel from the admin Firm Dashboard (`src/components/Compliance.jsx`) — **PJ-approved Option A**, frontend-only, **no new view / migration / RPC / Supabase object**.

- **G-11 / R-10 — CLOSED PASS.** Source commit `1a0f4bca78cbd2d6d8a659009a370e2784f17589` (branch `sync/integration`). Tests **336/336**; build PASS. **Live-verified on an authorised V2/yav2-dev Preview** (`ogjrwemjefvccpyjwxuo`): admin Firm Dashboard loads, "Team Workload" panel absent, Category Breakdown full-width, **DevTools Network 0/25** `v_team_workload` requests, **no** `/rest/v1/v_team_workload` request or related 404, and the Home Dashboard's **separate** client-side "Team Workload (open tasks)" feature (`src/components/Dashboard.jsx`, unchanged) still present.
- **Preview-only:** **NOT Production, NOT V1**; no deployment promotion, no alias reassignment, no SQL, no migration, no Supabase mutation, no PR/merge.
- **Evidence:** `supabase/verification/remediation-g11/G11_EXECUTION_SOURCE_CLOSEOUT.md`, `G11_POST_DEPLOYMENT_RUNTIME_EVIDENCE.md`, `G11_REMEDIATION_DESIGN.md`.
- **Overall-project completion:** per this register's per-package methodology, not re-weighted here. The separate governing weighted model (BASELINE.md §5) records the G-11 increment as **43.40% → 43.70%** (**+0.30 pp**; Area 2 Application/source +0.20, Area 7 Runtime +0.10), displayed **43.7%**, in `remediation-g11/G11_EXECUTION_SOURCE_CLOSEOUT.md`; **that weighting is neither re-derived nor changed here.**

## Core Operational Workflow Closure (branch `feature/yav2-core-operational-closure`)

Repository-only (frontend) reliability hardening of the end-to-end operational workflow (client → onboarding → tasks/follow-up → compliance → documents → team/access → audit/error). Cut from `sync/integration` @ `c4babefdcd50ce2d23005e597f27ba0d41b9674a`. **No backend touched** (no SQL/migration/RLS/RPC/permission/storage/mutation; V2/yav2-dev only, V1/Prod untouched). The paused presentation/redesign package (`feature/yav2-professional-redesign-launch`, PR #48) was not touched.

- **Status:** MERGED into `sync/integration` — PR #49 merged 2026-08-02 at merge commit `d8d1d9e2e925ca12010b6d56788d26a0e82ea25d` (PR head `395562338bd93e4240ea69875a85750007263ab0`); governing branch verified 399 tests / build clean post-merge. **Not deployed.**
- **Scope closed (repository-level):** count correctness (`Filed / Completed` tasks no longer counted open/overdue and now counted completed; local-date `todayLocal()` replaces UTC `toISOString` in Dashboard/Tasks; invalid-date guards); false-success + double-submit hardening on the task/follow-up/mark-filed write paths (checks Supabase `error` before success; re-entrancy guards); broken recovery path closed (`ResyncButton` now rendered on the Clients detail modal); shell resilience (App bootstrap try/finally+catch so it can't hang; root wrapped in `ErrorBoundary` so a shell render error no longer blanks the page); crash-safety null guards (Clients search, Team member name, GST period, Mark-Filed client, DocumentManager user); storage-orphan cleanup on WorkDocuments insert failure; business-safe error text via `safeErrorMessage` (Usage/ErrorBoundary/Clients PIN/MarkFiled/DocumentsHub).
- **Files (exact):** source **17** · existing tests amended **1** · new tests **1** (`tests/coreOperationalClosure.test.js`, **16** tests) · docs **3** · **total 22**.
- **Tests/build:** **383 → 399 pass / 0 fail**; `vite build` exit 0; browser boot renders Login (not blank), 0 console errors.
- **Backend dependencies (recorded, NOT executed):** (1) client-ID allocation race → DB sequence/RPC; (2) `task_id`/`followup_id` uniqueness → DB unique constraint (repository mitigation = re-entrancy guard + insert-error surfacing; the earlier format-changing suffix was **reverted** — external format consumers unverified); (3) role enforcement for compliance Mark-Filed and Clients edit/reset-PIN → RLS (UI guards are defence-in-depth). Bundle into governed P7/P8/P9.
- **Independent-review corrections applied:** ID format reverted (external consumers unverified); Follow-up and Mark-Filed made retry-idempotent (no duplicate follow-up log / no re-upload/re-file/duplicate document on retry) with accurate partial-success messages and preserved discoverability/recovery (tests CO-15/CO-16); no backend transaction/RPC added.
- **Manual verification dependency:** interactive authenticated smoke (Admin + non-Admin) needs a test-account password (governed reset out of scope).
- **Deferred repository items (next package):** ~~Compliance overdue-consistency unification across tabs + per-loader error states; WorkDocuments viewDoc/file-type/loading/`maybeSingle`~~ **→ CLOSED in the Compliance Reliability and Consistency Closure package (below).** Remaining deferred: Login/ChatAgent minor; Usage-tab visibility (PJ product decision).
- **Completion views (this package; overall weighted figure owned by the separate governing model, last ≈ 43.7%):** work-item ≈ 27/47 repository defects closed (~57%; **HIGH/critical ≈ 100%**); weighted **PROVISIONAL ESTIMATE ONLY ≈ 45.0%**, pending authoritative recalculation in the governing weighted model (not final/approved, not re-derived here); time-estimate ≈ 55–60% of identified core-flow repository hardening. Evidence: `docs/YAV2_CORE_OPERATIONAL_CLOSURE_REPORT.md`, `docs/YAV2_CORE_OPERATIONAL_CLOSURE_TEST_EVIDENCE.md`.

## Compliance Reliability and Consistency Closure (branch `feature/yav2-compliance-reliability-closure`)

Repository-only (frontend) functional reliability + consistency hardening of the Compliance module and the document components. Cut from `sync/integration` @ `53d14b73c90bdc7d412b92f44b5d9781179a5235`. **No backend touched** (no SQL/migration/RLS/RPC/permission/storage/mutation; V2/yav2-dev only, V1/Prod untouched). The paused presentation/redesign package (`feature/yav2-professional-redesign-launch`, PR #48) was not touched. Closes the compliance/document items explicitly deferred by the Core Operational Workflow Closure above.

- **Status:** **MERGED into `sync/integration`** — PR #51 merged 2026-08-02 at merge commit `768e06108c148969524edebe3b6a7858a7bb9e31` (PR head `3b8111f0f035a0a66b6a18368ff94cdec1adbfa5`); governing branch verified **424 tests pass / 0 fail** and `vite build` clean post-merge. **Not deployed.** Independent-review corrections (status semantics + strict/offset date validation) applied before merge.
- **Scope closed (repository-level):** (1) **one overdue/closed truth** — new pure `src/lib/compliance.js` helpers (`CLOSED_COMPLIANCE_STATUSES`, `MODULE_TERMINAL_STATUSES`, `COMPLETED_COMPLIANCE_STATUSES`, `isComplianceClosed`, `isComplianceCompleted`, `terminalStatusesFor`, `effectiveDueDate`, `complianceDateMeta`, `isComplianceOverdue`, `complianceRowGroup`); local-date string compare so **due-today is not overdue**, calendar-impossible dates round-trip-rejected, backend vocabulary preserved. Removed the UTC/local-mixing `new Date(eff(r)) < new Date()` cell math (IT/TDS/ROC/Audit), Notice's no-status-exclusion overdue, and ActivityView's 3-way date-field + divergent closed-set mismatch (filter/stat/badge now agree; the Overdue chip refines the server pre-filter client-side). `FileBtn` no longer offers Mark-Filed on a terminal-status row. (2) **~15 loaders** now capture the query `error` and render a retryable `<Err>` distinct from empty (was universal false-empty; ClientPanel coverage error had been silently **hiding real tabs**; FirmDashboard all-zero-on-outage read as "nothing overdue"). (3) **FinancialReviewModal** false-success closed — all three writes were discarded and `onDone()` fired regardless (silent "Reviewed" on failure); now re-entrancy guard + checked writes + tracker flips last + accurate partial-write message + busy-close protection. (4) **FinancialUploadModal / extraction** re-entrancy + busy-close + business-safe errors. (5) **WorkDocuments** load/view errors surfaced (viewDoc was a silent dead click), JS file-type allow-list, `.limit(1).maybeSingle()` dup-check, null-safe `uploaded_by`, checked deletes; **DocumentsHub/DocumentManager** checked deletes + business-safe errors (no raw `error.message`).
- **Files (exact):** source **5** (`src/lib/compliance.js`, `Compliance.jsx`, `WorkDocuments.jsx`, `DocumentsHub.jsx`, `DocumentManager.jsx`) · existing (pre-package) tests amended **0** · new tests **1** (`tests/complianceReliabilityClosure.test.js`, **25** tests) · docs **3** · **total 9**.
- **Tests/build:** **399 → 424 pass / 0 fail**; `vite build` exit 0 (125 modules); `git diff --cached --check` clean; staged-diff secret scan clean.
- **Independent-review corrections (Correction 1–3):** `Uploaded`/`Reviewed` are **not** globally terminal — `compliance_status_enum` + `v_client_compliance_summary` prove `Reviewed` is `review_pending` (mid-workflow) and the backend terminal set is `Filed/Completed/Closed/Not Applicable`; the global set is now **conservative** and `Uploaded`/`Reviewed` are terminal for the **financials** module only (`MODULE_TERMINAL_STATUSES`, threaded via `moduleKey`/`act.id`), so a Reviewed-but-not-Filed standard-tracker row stays pending, can be overdue, keeps its filing action and is not counted completed. The date-key helper is now **strict full-string** (whole value must be an exact `YYYY-MM-DD` or a fully-valid ISO datetime — not accepted on a valid prefix alone, so `2026-08-02garbage`/`…-invalid`/`…Tnot-a-time`/`…T99:99:99` are rejected), **round-trip-validates** real calendar dates (rejects `2026-13-01`/`2026-02-30`/`2026-00-10`/`2026-04-31`/non-leap `2025-02-29`), and **range-checks the timezone offset** (mm `00–59`, hh `00–14`, `14:mm`⇒`mm=00`; `+14:00` valid, `+14:30`/`+24:00`/`+99:99`/`-15:75` rejected). Tests strengthened to executable pure-logic for module semantics, completed-vs-pending, calendar validity (incl. leap day) and boundary dates.
- **Backend dependencies (recorded, NOT executed):** (1) `compliance_calendar` idempotency needs a `(client_id, compliance_tracker_id)` unique constraint + upsert (frontend guard not race-proof); (2) the overdue/closed-set + date-field logic **inside** `v_client_compliance_summary` / `v_firm_dashboard` / `v_overdue_ageing` must be reconciled to match the now-consistent frontend rule — a likely count-vs-badge gap at the view layer; (3) role enforcement for Mark-Filed/Review → RLS (UI gating is defence-in-depth). Bundle into governed P6/P8/P10.
- **Manual verification dependency:** interactive authenticated boot smoke needs yav2-dev test-account credentials + a git-ignored `.env.local` (absent in this worktree); build (all modules transformed) + 424 pure/static tests are the in-repo runtime verification.
- **Completion views (this package; overall weighted figure owned by the separate governing model, last ≈ 45.0%):** work-item ≈ **all repository-fixable compliance/document reliability items closed** (0 deferred to a further repository pass; remaining is the recorded backend view-alignment + constraint); **HIGH/critical ≈ 100%** (financial-review false-success + false-empty-on-outage classes closed); weighted **PROVISIONAL ESTIMATE ONLY ≈ 45.0%**, pending authoritative recalculation in the governing weighted model (not final/approved, not re-derived or inflated here). Evidence: `docs/YAV2_COMPLIANCE_RELIABILITY_CLOSURE_REPORT.md`, `docs/YAV2_COMPLIANCE_RELIABILITY_TEST_EVIDENCE.md`.

## Client 360° Operational Workspace (branch `feature/yav2-client-360-operational-workspace`)

Repository-only (frontend) functional feature package: a per-client Client 360° operational workspace (header, operational-summary cards, attention-required panel, and nine tabbed sections — Overview, Compliance, Tasks, Follow-ups, Documents, Financials, Notices, Team & access, Recent activity). Cut from the **verified post-PR-#51** `sync/integration` @ `768e06108c148969524edebe3b6a7858a7bb9e31` (NOT the older `53d14b7…`). **No backend touched** (no SQL/migration/RLS/RPC/permission/storage/mutation; V2/yav2-dev only, V1/Prod untouched). The paused presentation/redesign package (`feature/yav2-professional-redesign-launch`, PR #48) was not touched.

- **Status:** **MERGED into `sync/integration`** — PR #53 merged 2026-08-03 at merge commit `9d4d0ecfb0f2fa4b044e2151170067b379ad4aad` (final feature head `ef546e639b285fec2f2242340fa60d2e5751d2a4`); **UAT PASS** (`docs/YAV2_CLIENT_360_UAT_RESULT.md`, client YA-013; UAT-01/UAT-02 corrected + retested); post-merge **477 tests pass / 0 fail**, `vite build` clean. **Not deployed.** Ships dark behind `VITE_CLIENT360_UI` (Admin/Manager only). PR #48 untouched.
- **Scope delivered (repository-level):** read-first workspace that **reuses the PR#51 shared compliance truth** (`complianceDateMeta`/`isComplianceClosed`/`isComplianceCompleted` + `todayLocal`) to classify `compliance_calendar` rows — no second definition of overdue/due-today/terminal; reuses `isTaskClosed`/`isTaskCompleted`/`getDueMeta` for tasks and `currentFy`/`startFyFromDate` for FY; adds the **first shared follow-up helper** (lifted from the `Tasks.jsx` inline predicate, with an open-task guard). Per-panel independent loading/empty/error/refresh — **one panel failing never blanks the page and a failed load is surfaced as a critical attention item, never a clean/empty read**. Correct dual client-key discipline (uuid for compliance/notices; text YA-code for tasks/follow-ups/documents/financials). Quick actions reuse existing permission-checked flows (AddTaskModal via a new backward-compatible `presetClient` prop; OnboardingWizard; embedded DocumentManager). Fail-closed role gating + safe restricted notice (no raw errors); accessible dialog/tabs/tables.
- **Files (exact):** new source **7** (`src/lib/client360.js`, `src/services/client360Reads.js`, `src/hooks/useClient360Role.js`, `src/hooks/useClient360Data.js`, `src/components/client360/Client360Primitives.jsx`, `Client360Sections.jsx`, `Client360Workspace.jsx`) · modified source **3** (`Clients.jsx` launcher, `AddTaskModal.jsx` optional `presetClient`, `.env.example` flag) · new tests **1** (`tests/client360.test.js`, **53** tests) · docs **3** (report + UAT result + this register).
- **Tests/build:** **424 → 477 pass / 0 fail** (53 Client 360 tests incl. UAT-01/UAT-02 corrections); `vite build` success (132 modules); `git diff --check` clean; staged-diff secret scan + prohibited-file scan clean; post-merge re-verified 477 pass / build clean on `sync/integration` @ `9d4d0ec`.
- **Backend dependencies (recorded, NOT executed):** (1) a client↔team / relationship-manager assignment field/table (Team & access is derived from task assignees today); (2) name-resolution for `compliance_calendar.assigned_to` / `notice_tracker.assigned_to` UUIDs; (3) a unified per-client activity aggregation (general `audit_log` write path is deferred); (4) an optional required-document checklist model (today "missing documents" = zero documents, per AdminHome).
- **Manual UAT (COMPLETED):** live UAT performed by PJ in the authorised yav2-dev environment (`VITE_CLIENT360_UI=true`) for client **Rupesh & Co (YA-013)** — launcher/open/header/14 cards/9 tabs verified, missing values render `—`, no raw `null`/`undefined`/backend error, Financials tab loaded 5 FY 2026-27 rows. Two findings raised and both **corrected + retested**: **UAT-01** (attention message line-through removed; clear underline affordance retained) and **UAT-02** (financial wording made neutral — "Financial documents pending" / "pending action" — `Not Uploaded` no longer implies "review"). **UAT result: PASS.** Evidence: `docs/YAV2_CLIENT_360_UAT_RESULT.md`. Remaining genuine limitation: exhaustive multi-role live click-through beyond the confirmed YA-013 session is covered by source/tests, not a second live pass.
- **Governance status:** **CLIENT 360 TECHNICAL IMPLEMENTATION CLOSED — GOVERNANCE FINALISATION IN PROGRESS.** Feature PR #53 MERGED (`9d4d0ec`, feature head `ef546e6`); this register status-flip is PR #54 (docs-only). The authoritative whole-project completion % is owned by `YAV2_Whole_Product_Completion_Blueprint.xlsx` and is being recalculated by the workbook's own formulas in this 2026-08-03 closure cycle — **the Excel blueprint is not asserted updated here** (its final figures + the dated closure note record the recalculation). No hand-derived headline % is claimed in this register. Evidence: `docs/YAV2_CLIENT_360_OPERATIONAL_WORKSPACE_REPORT.md`, `docs/YAV2_CLIENT_360_UAT_RESULT.md`.

## Client Lifecycle & Work Management Closure (branch `feature/yav2-lifecycle-work-management-closure`)

Repository-only (frontend) reliability + consistency hardening of the EXISTING work-management (`Tasks.jsx`) and client-lifecycle (`Clients.jsx`) surfaces. Cut from `sync/integration` @ `a59d0e26548b8eaa7fea5ff8143ea98b868b3563`. **No backend touched** (no SQL/migration/RLS/RPC/permission/storage/mutation; V2/yav2-dev only, V1/Prod untouched). **No net-new UI surface and no new write path.** Delivered under active machine-enforced autonomous safety controls (separate infra PR #56). PR #48 (paused redesign) untouched.

- **Status:** **MERGED into `sync/integration`** — PR #57 merged 2026-08-03 at merge commit `28faf2f14cb905e04cd836f26d84b4b04df9a230` (final feature head `85254c0a4a433b3b8f785e38a08207f5de95c872`); post-merge governing verified **489 tests / 0 fail** and `vite build` exit 0. **PJ authenticated live UAT PASS (all 22 cases).** GitHub CI ("install + test + build") green. **Not deployed.** Explicitly non-overlapping with PR #49 (task/follow-up/mark-filed write paths), PR #51 (compliance loaders + shared overdue/closed truth), and PR #53 (Client 360 + shared follow-up helper) — those are recorded as done, not redone.
- **Scope closed (repository-level):** **WM-1 (HIGH)** — `Tasks.load` false-empty-on-outage closed (each query `error` checked; retryable error state distinct from empty, mirroring `Clients.jsx`); **WM-2** exact assignee filter (was substring `.includes`); **WM-3** shared, tested follow-up ageing (`isFollowUpToday`/`isFollowUpOverdue`, due-today-not-overdue, local-date); **WM-4** non-owner Mark-Done routed to inline `actionError` (no blocking `alert`). **CL-1** directors fetch error-surfacing + stale-response race guard (legacy `c.directors` fallback preserved); **CL-2** consistent lifecycle-status representation (`clientStatusLabel`; no misleading `null→Active`; blank→`Unknown`) + read-only status filter on the register; **CL-3** removed the duplicate Escape `window` keydown listener (kept `useEscapeKey`). New shared pure helpers in `src/helpers.js`.
- **Review corrections applied:** (1) directors-map now **clears the cached entry on an empty result** (pure `nextDirectorsMap` — no stale rows survive; legacy `c.directors` fallback preserved); (2) `Tasks.load` wrapped in **try/catch/finally** so a rejected request/exception also triggers the retryable error state and `loading` is always reset.
- **Files (exact):** source **3** (`src/helpers.js`, `src/components/Tasks.jsx`, `src/components/Clients.jsx`) · existing tests amended **0** · new tests **1** (`tests/clientLifecycleWorkManagementClosure.test.js`, **12** tests) · docs **4** (this register + closure report + test evidence + spec) + plan.
- **Tests/build:** **477 → 489 pass / 0 fail**; `vite build` exit 0; non-auth boot serves HTTP 200 with the app shell.
- **Backend dependencies (recorded, NOT executed):** (1) client-ID allocation race → DB sequence/RPC; (2) `task_id`/`followup_id` uniqueness → DB unique constraint; (3) role enforcement for the deferred status-transition control + Mark-Done → RLS. Bundle into governed P7/P8/P9.
- **Deferred (recorded, not built):** a guarded client status-transition control (Active↔Inactive/Archive) — a new write path, out of scope this package (filter + read-only display only).
- **Live verification COMPLETE:** executor non-auth boot check PASS (HTTP 200, 0 console errors, V2/yav2-dev); **PJ authenticated live UAT PASS — all 22 cases, no findings** (2026-08-03; authorised V2 Admin/Manager). Pending-UAT blocker cleared. Evidence: `docs/YAV2_CLIENT_LIFECYCLE_WORK_MANAGEMENT_UAT_RESULT.md`.
- Evidence: `docs/YAV2_CLIENT_LIFECYCLE_WORK_MANAGEMENT_CLOSURE_REPORT.md`, `docs/YAV2_CLIENT_LIFECYCLE_WORK_MANAGEMENT_TEST_EVIDENCE.md`, `docs/YAV2_CLIENT_LIFECYCLE_WORK_MANAGEMENT_UAT_RESULT.md`, `docs/superpowers/specs/2026-08-03-yav2-safety-controls-and-lifecycle-work-closure-design.md`, `docs/superpowers/plans/2026-08-03-yav2-client-lifecycle-work-management-closure.md`.

## Autonomous Safety Controls (branch `chore/yav2-autonomous-safety-controls`, PR #56)

Repository-level infrastructure (separate from the functional closure). A Claude Code `PreToolUse` hook (`.claude/hooks/guard.mjs`) machine-enforces YAV2 governance: **deny** the V1/Prod Supabase ref, Supabase backend-mutation MCP tools, irreversible git/filesystem operations, and moving the tree onto the default branch; **ask** (human approval) for deployment/promotion and for integrating/pushing to a governing branch; **allow** everything else. Fails safe (ask) on parse/internal error. Validated: `node --check`, 35/35 simulation matrix, live positive/negative controls, `claude doctor` clean, hot-reloaded (no restart). **Status:** **MERGED into `sync/integration`** — PR #56 merged 2026-08-03 at merge commit `2ef98e3d3af524b002e29c52bd893117d1cc697b`. Also registered globally at `~/.claude/settings.json` (protection across repos) with the PJ Autonomous Working Policy in `~/.claude/CLAUDE.md`. Doc: `docs/YAV2_AUTONOMOUS_SAFETY_CONTROLS.md`.

## Authentication & Session Resilience Closure (branch `feature/yav2-auth-session-resilience-closure`)

Repository-only (frontend) reliability hardening of the sign-in / session-restore / shell auth flow — the last explicitly-deferred core-shell items from the Core Operational Closure §9. Cut from `sync/integration` @ `61b6a9910557c2a4214350ee3be5dee7fd874ad5`. **No backend touched** (no SQL/migration/RLS/RPC/permission/storage/mutation; V2/yav2-dev only, V1/Prod untouched). Fail-closed security preserved. PR #48 (paused redesign) untouched.

- **Status:** Draft PR against `sync/integration` — **not merged, not deployed.** Non-overlapping with PR #49/#51/#53/#57 (those hardened workflow/compliance/client360/lifecycle; the Login/loadUser/ChatAgent auth items were explicitly deferred, recorded as done there).
- **Scope closed (repository-level):** **AS-1 (HIGH)** `Login.handleLogin` — a transient post-sign-in `team`-lookup error no longer masquerades as "account not active"; new shared `classifyMembership` (fail-closed) yields a retryable message, access still denied. **AS-2 (MED)** `App.loadUser` — a transient session-restore error shows a retryable "Couldn't verify your session" screen (Retry) instead of silently signing a valid user out; only `granted` admits. **AS-3 (MED)** `App.handleSetNewPassword` — error routed through `safeErrorMessage` (no raw `error.message`). **AS-4 (LOW)** `ChatAgent` — analysing-status timer held in a ref and cleared (no stale `setThinkingText`). **AS-5 (LOW)** `Login.handleForgot` — transport errors handled, anti-enumeration preserved. New `src/lib/authSession.js`.
- **Files (exact):** new source **1** (`src/lib/authSession.js`) · modified source **3** (`Login.jsx`, `App.jsx`, `ChatAgent.jsx`) · new tests **1** (`tests/authSessionResilienceClosure.test.js`, **5** tests) · existing tests amended **1** (`tests/appShellRuntime.test.js` R5 → new fail-closed shape) · docs **4** (report, test evidence, UAT checklist, this register) + spec + plan.
- **Tests/build:** **489 → 494 pass / 0 fail**; `vite build` exit 0; non-auth boot check PASS (Login renders, HTTP 200, 0 console errors, V2/yav2-dev).
- **Backend dependencies (recorded, NOT executed):** active-member enforcement is `team`-table + RLS authority (UI resilience is defence-in-depth); an optional membership RPC returning an explicit transient-vs-denied signal is deferred.
- **Live verification COMPLETE:** non-auth boot check PASS (Login renders, HTTP 200, 0 console errors, V2/yav2-dev); **PJ authenticated live UAT PASS — no findings** (2026-08-03; authorised V2 Admin/Manager; transient-error simulation confirmed retryable, no false lockout, fail-closed preserved). Manual-UAT blocker cleared. Checklist: `docs/YAV2_AUTH_SESSION_RESILIENCE_UAT_CHECKLIST.md`.
- Evidence: `docs/YAV2_AUTH_SESSION_RESILIENCE_CLOSURE_REPORT.md`, `docs/YAV2_AUTH_SESSION_RESILIENCE_TEST_EVIDENCE.md`, `docs/YAV2_AUTH_SESSION_RESILIENCE_UAT_CHECKLIST.md`.

## Risk register

| ID | Risk / blocker | Severity | Position |
|---|---|---|---|
| B-1 | **BLOCKER.** Supabase MCP in this environment exposes ONLY V1/Production `zcszesuvjrryxtigjglt`. All MCP DB use stopped per Global Rule 2; zero queries run. Standing hazard for any future session; also blocks live verification. **RULED 2026-07-18: execution blocker only — never query it; local authoring continues; live verification/execution via PJ-manual V2 SQL Editor unless a safe V2-only connector appears.** | Critical | RULED / STANDING |
| B-2 | **BLOCKER (governance).** Out-of-band `0017_m1b_d2b…sql` appeared in the working tree during P0 (2026-07-18 07:03:38 IST), not authored by this session, provenance unknown (possible concurrent session). Collides with P1 scope and numbering. **RULED 2026-07-18: provenance confirmed (earlier D2b session) — classified D2b Rev 1, HOLD / REVISION REQUIRED; retain, never delete/execute. See D-P0-1 for post-P0 concurrent changes.** | High | RULED |
| B-3 | `.git/info/exclude` hides `supabase/` from git status; migrations 0001–0011 and 0014 exist on disk only (no commit, no push) — evidence-chain and disaster-recovery gap, and the mechanism that let B-2 arrive silently. **RESOLVED 2026-07-18: exclude line removed per ruling (backup `.bak-p0`); tree classified (see inventory); corpus commit deferred pending hygiene-plan approval.** | High | RESOLVED (commit pending) |
| R-4 | Preview deployment (READY) is pinned to `ae6bf1e0`, 13 commits behind working HEAD; recent safety fixes not deployed. Redeploy is a material-behaviour Preview change → needs gate approval. | Medium | OPEN |
| R-5 | Staged-but-uncommitted D2a evidence (4 files) — closed-gate evidence not yet in history. Low-risk housekeeping commit recommended at P0 close-out. | Low | OPEN |
| R-6 | **Rule conflict.** Master Rule 6 forbids storing Aadhaar last-four, but live data (`clients.directors` JSONB) and current UI store `aadhaar_last4`/`aadhaar_masked` (mask embeds the last four). Affects P1 (RPC field rejection), P2 (UI capture removal), P3 (backfill must drop). **RULED 2026-07-18: no capture/accept/backfill/new storage of Aadhaar digits incl. last-four anywhere in V2; legacy source fields NOT destructively deleted in P1/P3; legacy cleanup separately governed.** | High | RULED |
| R-7 | Frontend keys client writes on `clients.client_id` business code (OnboardingWizard, Clients doc_pin); Rule 7 requires UUID relational keying — P2 remediation scope. | Medium | OPEN |
| R-8 | Broad direct-table-write surface outside Module 1 (tasks, documents, trackers) remains unaudited until P7/P8; interim exposure accepted by phasing. | Medium | ACCEPTED (phased) |
| R-9 | Numbering 0012 = secure-docs executed live but file ABSENT from repo (recover/re-author at a future gate); 0013 reserved for R3. Do not reuse either number. | Info | NOTED (corrected) |
| R-10 | **G-11 — `v_team_workload` missing.** Unauthored/absent live; `src/components/Compliance.jsx` queried it → 404 on the admin Firm-Overview load; the "Team Workload" panel rendered empty (graceful degradation, no crash) — a **Medium visible functional defect with graceful degradation**. **CLOSED PASS via Option A** (query + always-empty panel removed; no new view/migration/RPC/Supabase object) at source commit `1a0f4bca78cbd2d6d8a659009a370e2784f17589`; tests 336/336; **live-verified on an authorised V2 Preview** (0/25 `v_team_workload` requests, no related 404, Firm Dashboard renders, Home Dashboard's separate workload feature unaffected). Preview-only — **not Production, not V1, no alias change**. Evidence: `supabase/verification/remediation-g11/G11_EXECUTION_SOURCE_CLOSEOUT.md`, `G11_POST_DEPLOYMENT_RUNTIME_EVIDENCE.md`, `G11_REMEDIATION_DESIGN.md`. Unrelated to the T4 security hardening. | Medium | **CLOSED PASS** (Option A, live-verified V2 Preview) |
| R-11 | **FY 2026-27 backend-generation limitation hypothesis — SUPERSEDED BY LIVE DIAGNOSIS.** The 42-block P6 SELECT-only diagnosis verified that `financial_years` includes FY 2026-27, `get_current_fy()` returns FY 2026-27, the reviewed generators run through `get_current_fy()`, and YA-012 contains FY 2026-27 income-tax, accounting and financials records. The remaining open issue is stale or misleading **frontend warning/logic** and the broader applicability-driven P6 design; **backend FY 2026-27 support itself is VERIFIED LIVE.** Evidence: `docs/M1B_P6_SELECT_Only_Diagnosis_Closure_Report.md`. | Medium | SUPERSEDED / FRONTEND CORRECTION OPEN |

## Proposed migration numbering

| Number | Content | Gate |
|---|---|---|
| 0017 | M1-B D2b CRUD RPCs + INSERT/UPDATE bypass closure (+ `_rollback`) — **EXECUTED / CLOSED PASS** | P1 |
| 0018 | M1-B D2b DELETE privilege closure (+ `_rollback`, read-only verification) — **EXECUTED / CLOSED PASS** | P1 |
| 0019 | D3 legacy person backfill package (renumbered from 0018) | P3 |
| 0020 | D4 remediation-flag population (renumbered from 0019) | P4 |
| 0021 | Service applicability schema + reference seed + RPCs — **EXECUTED / CLOSED PASS** (V2/yav2-dev, 2026-07-19) | P5 |
| 0022 | **P5 PG-1 — OTHER-notes authoritative enforcement (table CHECK `csa_other_notes_required_chk` + create/update RPC guards) — EXECUTED / CLOSED PASS** (V2/yav2-dev, 2026-07-21) | P5 (PG-1) |
| 0023 | Controlled compliance generation (**moved from 0022**) | P6 |
| 0024+ | P7/P8/P9 backend objects, sequential, one gate per number | P7–P9 |

0012 (secure-docs, executed live, file absent) and 0013 (reserved, R3) are not reusable; 0014–0016 frozen.

---

*Register update rule: this file is amended at the close of every gate with status, commit, evidence hashes and Preview URL. This Rev 1.0 records the P0 result and awaits independent review.*
