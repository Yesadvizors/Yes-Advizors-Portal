# DRAFT — Master Completion Register update for P6A (NOT COMMITTED)

> **This is a DRAFT proposed insertion for `docs/YAV2_Master_Completion_Register.md`. It is deliberately kept as a separate file and is NOT applied to the committed register, per the mandatory stop point. For PJ/ChatGPT review only.**

## Proposed new entry under "P6 — Controlled compliance generation"

- **P6A — Stale frontend financial-year warning/logic correction — AUTHORED / READY FOR INDEPENDENT REVIEW (repository-only; NOT COMMITTED).**
  Addresses diagnosis correction **#1** and risk **R-11** (the residual frontend warning/logic item left open after the P6 SELECT-only diagnosis superseded the FY-2025-26 backend-cap hypothesis). **Root cause:** `src/lib/financialYear.js` was authored against migration `0008` (hard-coded `<= '2025-26'` ceiling; silent success over an empty range) and carried a frozen `BACKEND_MAX_FY = '2025-26'`; migration `0014_r4db_financial_year_repair.sql` later replaced that ceiling with `get_current_fy()` and made an empty FY range an explicit error, but the frontend was never updated — so once the real FY became **2026-27** the frozen constant made `fyCoverage()` falsely warn *"the database can only generate up to FY 2025-26 … needs a database update"* on every non-draft save and every Re-sync. **Change (frontend-only code change — 4 implementation/test files changed; plus 3 review-document files added under `docs/p6a-frontend-fy-correction/`, for a 7-file working-tree package):** removed `BACKEND_MAX_FY`; made `fyCoverage()`'s ceiling default to the dynamic current FY (aligned with `get_current_fy()`); preserved the fail-closed branch and the explicit-ceiling gap-detection mechanism; drove `Clients.jsx` and `OnboardingWizard.jsx` coverage messaging from `coverage.reason` (removed all copy asserting the removed ceiling); rewrote the stale `0008`-era comments to cite `0014` + the diagnosis; updated/extended `tests/financialYear.test.js` with corrected-behaviour, fail-closed, FY-2025-26-still-valid, and a STATIC **regression guard** against reintroducing any frozen FY cap. **Verification:** `node --test` **342/342 PASS** (baseline 336); `vite build` clean (exit 0). Type-check/lint **N/A** (no TS/ESLint toolchain in repo); `scripts/verify-supabase-ref.mjs` correctly refused (exit 2, no `.env`) — no Supabase access. **Files (7 total = 4 changed + 3 added):** *changed* — `src/lib/financialYear.js`, `src/components/Clients.jsx`, `src/components/OnboardingWizard.jsx`, `tests/financialYear.test.js`; *added (docs, untracked)* — `docs/p6a-frontend-fy-correction/P6A_REVIEW_PACKAGE.md`, `..._IMPACT_ASSESSMENT.md`, `..._MASTER_COMPLETION_REGISTER_DRAFT.md`. **Boundaries:** no commit/push/PR/merge/deploy; no Supabase/SQL/migration; no RLS/grant/DB-contract change; no Production/alias change; no P6B/P6C/P6D work. **Governing baseline:** fresh clone at `D:\Claude\Claude Code\Yes-Advizors-Portal-P6`, branch `p6/frontend-fy-warning-correction` cut from `origin/sync/integration @ c0009fc9cca61d5aa716c4e6e1c3ea6ab6ef54d5` (verified exact). **Status: AUTHORED; independent ChatGPT review PENDING; PJ approval and commit UNAUTHORISED / not performed.** Evidence: `docs/p6a-frontend-fy-correction/P6A_REVIEW_PACKAGE.md`, `..._IMPACT_ASSESSMENT.md`.

## Proposed risk-register update

- **R-11** (frontend FY warning/logic correction): change position from **OPEN** to **AUTHORED — CORRECTION PACKAGE READY FOR REVIEW (not committed)**. Retain OPEN status on the *deployment/verification* aspect until independent review, PJ approval, commit, and the required live V2 SELECT-only confirmation (§11 of the review package) are complete.

## Notes for the register maintainer (do not lose these on merge)

- The committed register's "Closed baseline" line — *"R4-DB migration 0014 … CLOSED / PASS — never modify"* — is consistent with this package: `0014` is **not** touched; the frontend is merely brought into line with it.
- The remaining `'2025-26'` references in the backend (`get_unknown_incorporation_start_fy()` start-policy anchor) are an **intentional business rule (a floor, not a ceiling)** and were **deliberately not changed**.
- Protected counts and all backend contracts are unaffected (no writes, no schema, no SQL).
