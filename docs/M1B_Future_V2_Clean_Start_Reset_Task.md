# Future Controlled Task — **V2 Clean-Start Reset before real onboarding**

**Status: PROPOSED / NOT AUTHORED / NOT EXECUTED / NOT SCHEDULED.** This is a placeholder
for a future, separately-reviewed task. **Nothing here is to be authored or executed now.**
No SQL, no Supabase connection, no MCP, no data deletion is performed by this document.

**Authorised project (when eventually done):** V2/yav2-dev `ogjrwemjefvccpyjwxuo` **only**.
V1/Production `zcszesuvjrryxtigjglt` is strictly prohibited and out of scope.

---

## 1. Why

All 26 legacy `public.clients.directors` JSONB entries — and any other rows created purely
for development/testing — are **sample/testing data** (PJ-confirmed). They must be removed
before real client onboarding, via a **single, reviewed, controlled clean-start reset**,
**after** implementation and testing are complete. Migration 0019 (the legacy-person
backfill) was **cancelled** because there is nothing real to preserve.

## 2. When

Only **after** M1-B (and any dependent modules) are implemented, tested, and signed off —
immediately **before** real onboarding begins. Not before.

## 3. Scope — DELETE (sample business data only)

The reset removes only **sample/testing business data**, e.g. sample `clients` (incl. their
`directors` JSONB), and any sample rows in dependent business tables (persons, registrations,
identifiers, contacts, addresses, trackers, work/documents, remediation flags, audit rows
generated by testing, etc.). The exact table list and per-table predicates must be
**enumerated and independently reviewed** when the task is authored.

## 4. Scope — PRESERVE (must NOT be deleted)

- database **schema and migrations**;
- **RLS policies**;
- **functions and triggers**;
- **reference / configuration data**;
- **compliance definitions**;
- **financial years**;
- **application code**;
- **deployment configuration**;
- **PJ's approved admin access**.

## 5. Controls required when authored (not now)

- explicit, reviewed table+predicate inventory of what is deleted vs preserved;
- ordered, FK-safe deletion within **one transaction** with pre/post row-count assertions;
- a dry-run / read-only preview (counts only, no values) approved before any delete;
- fail-closed aborts if any preserve-list object would be touched;
- V2-only guard; V1/Production untouchable;
- PJ is the sole approver and manual executor; Claude authors only, never executes.

## 6. Boundaries (now)

Do **not** author the reset SQL, connect to Supabase, use MCP, delete any data, commit,
push, deploy, or move anything into `supabase/migrations/`. This file is a forward pointer
only, to be picked up as a separate reviewed task after implementation + testing.
